# Exemplo de .env para MongoDB Atlas
MONGO_URI=mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_DB=<dbname>

# Substitua <usuario>, <senha>, <cluster> e <dbname> pelos seus dados do MongoDB Atlas.
# NÃO coloque espaços antes ou depois do sinal de igual (=).
# Exemplo real:
# MONGO_URI=mongodb+srv://rafaella:suasenha@cluster0.abcde.mongodb.net/loja?retryWrites=true&w=majority
# MONGO_DB=loja
